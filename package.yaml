name: quine-checker

ghc-options:
  - -Wall
  - -Wno-name-shadowing

dependencies:
  - shake

tests:
  spec:
    main: Spec.hs
    source-dirs:
      - test
      - src
    dependencies:
      - base
      - hspec
      - mockery
      - silently
